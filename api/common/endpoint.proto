syntax = "proto3";

package NYql.NConnector.NApi;

option go_package = "github.com/ydb-platform/fq-connector-go/api/common";

// TEndpoint represents the network address of a data source instance
message TEndpoint {
    string host = 1;
    uint32 port = 2;
}
