PRAGMA generic.UsePredicatePushdown="true";

pragma ydb.OverridePlanner = @@ [
    { "tx": 0, "stage": 0, "tasks": 32 }
] @@;


SELECT
    SUM(LEN(l_comment)),
    SUM(Datetime::GetDayOfYear(l_commitdate)),
    SUM(l_discount),
    SUM(l_extendedprice),
    SUM(l_linenumber),
    SUM(LEN(l_linestatus)),
    SUM(l_orderkey),
    SUM(l_partkey),
    SUM(l_quantity),
    SUM(Datetime::GetDayOfYear(l_receiptdate)),
    SUM(LEN(l_returnflag)),
    SUM(Datetime::GetDayOfYear(l_shipdate)),
    SUM(LEN(l_shipinstruct)),
    SUM(LEN(l_shipmode)),
    SUM(l_suppkey),
    SUM(l_tax)
FROM (SELECT * FROM external_datasource.olap_lineitem_s10 LIMIT 10000000);

